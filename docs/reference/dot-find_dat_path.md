# Locate a data file, installed or in the source tree

Locate a data file, installed or in the source tree

## Usage

``` r
.find_dat_path(filename, package)
```

## Arguments

- filename:

  File name with extension.

- package:

  Package name, possibly `""`.

## Value

The path, or `""` when the file is not there.
