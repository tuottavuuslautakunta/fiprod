# Package root, found by walking up to DESCRIPTION

Package root, found by walking up to DESCRIPTION

## Usage

``` r
.pkg_root(start = getwd())
```

## Arguments

- start:

  Directory to start from.

## Value

The package root, or `NA_character_` if no DESCRIPTION is found.
