% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oecd_funs.R
\name{oecd_clean_data}
\alias{oecd_clean_data}
\title{Clean and reshape OECD SDMX data}
\usage{
oecd_clean_data(sdmx_obj, vars = c(), freq = "A")
}
\arguments{
\item{sdmx_obj}{A SDMX object containing OECD SDMX data.}

\item{vars}{A named character vector specifying variables to keep and rename,
in the form \code{c(new_name = "old_name")}. The old names must exist in \code{df}.}

\item{freq}{Data frequency: \code{"A"} for annual data, \code{"Q"} for quarterly data.
Determines the date parsing format for the \code{obsTime} column.}
}
\value{
A tibble with renamed variables from \code{vars} and a \code{values} column.
}
\description{
This function parses dates, converts character columns to factors,
selects and renames specified variables, creates a \code{values} column
from \code{obsValues} (or \code{obsValue}), and drops all other columns.
It warns if any dropped factor variables have more than one level.
}
\examples{
\dontrun{
vars <- c(geo = "LOCATION", measure = "MEASURE")
cleaned <- oecd_clean_data(smdx_data, vars = vars, freq = "A")
}
}
